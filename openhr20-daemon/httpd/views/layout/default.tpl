    % import os
    % base_url = os.getenv("BASE_URL", default='/')
    % include('layout/head', title=title, base_url=base_url)
    % include('layout/header', page=page, base_url=base_url)
    % include('layout/main', base=base)
    % include('layout/foot')
